const submissions = [
    { name: `Jane`, Score: 95, date: 2020-01-24, passed: true},
    { name: `Joe`, Score: 77, date: 2018-05-14, passed: true},
    { name: `Jack`, Score: 59, date: 2019-07-05,passed: false},
    { name: `Jill`, Score: 88, date: 2020-04-22, passed: true}
]

function addSubmission(array, newName, newScore, newDate) {
    const newPerson = 
        {name: `Jackson`, Score: 86, date: 2020-03-25}
        passed = score >= 60 ? true : false;
}

array.push(newPerson); 

function deleteSubmission(array, index) {
    array.splice(4,1);
}
console.log(deleteSubmission);

function deleteSubmissionbyName(array, name) {
    submissions.findIndex(name);

    array.splice('Jackson');
}

function editSubmission(array, index, score) {

}

const findSubmissionbyName = array.find((array, name) => {
    return array.name === `Joe`
})

function findLowestScore(array) {
    array.forEach(myFunction);
    
    function myfunction(score) {
        arr[score] = score <= 60;
    }
}

function findAverageScore(array) {
    for (let array of findAverageScore);
    console.log(findAverageScore);
}

let result = array.filter(filterPassing);
function filterPassing (score) {
    return score >= 60;
}

let above= array.filter(filter90andabove);
function filter90andabove (array) {
    return score >= 90;
}

